import { StyleSheet } from "react-native";

const styles= StyleSheet.create({
    title: {
        fontSize:24,
        fontStyle: "italic",
        fontWeight: "bold",
        textAlign: "center"
    }
})

export default styles;